{
	"name": "har-compare-viz",
	"version": "0.0.1",
	"description": "Compare har files. Visually.",
	"main": "index.html",
	"type": "module",
	"scripts": {
		"pretest:watch": "echo 'TODO: start server in pretest:watch...'",
		"test:watch": "cypress open",
		"test": "cypress run --ci-build-id $BRANCH_NAME",
		"start": "node ./test-server.mjs"
	},
	"keywords": [
		"har",
		"test",
		"network",
		"timing"
	],
	"author": "Rahman Malik",
	"license": "MIT",
	"dependencies": {
		"chart.js": "^2.9.3"
	},
	"devDependencies": {
		"cypress": "^4.7.0"
	},
	"defaultConfig": {
		"TEST_SERVER_PORT": 8090,
		"TEST_SERVER_ADDR": "0.0.0.0"
	}
}
